- A "Person" class can have a "Dog" object as an attribute, so if the "Dog" class is not defined, we can not define the "Person" class.
- If the class "Person" has a constructor that does not pass by a "Dog" object, then we can define the class "Person" but the attribute "Dog" will be null.
